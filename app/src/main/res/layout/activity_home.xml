<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/home_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Contenu principal -->

    <!-- Navigation View (Drawer qui apparaît lorsque buttonMenu est cliqué) -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/white"
        android:padding="10dp">


        <!-- Bouton Menu (ouvre le DrawerLayout) -->
        <ImageButton
            android:id="@+id/buttonMenu"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_gravity="top|end"
            android:background="@android:color/transparent"
            android:contentDescription="@string/menu"
            android:scaleType="centerInside"
            android:src="@drawable/ic_menu"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/textViewPseudo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:paddingTop="20dp"
            android:text="@string/activity_main_title"
            android:textColor="@color/Title"
            android:textSize="32sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Votre CardView avec MapView -->

        <!-- GridLayout pour les boutons -->

        <GridLayout
            android:id="@+id/buttonContainer"
            android:layout_width="363dp"
            android:layout_height="126dp"
            android:layout_marginTop="72dp"
            android:columnCount="3"
            android:padding="10dp"
            android:scaleType="fitCenter"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/textViewPseudo">

            <Button
                android:id="@+id/buttonNonVisible"
                android:layout_width="70dp"
                android:layout_height="70dp"
                android:layout_columnWeight="1"
                android:layout_margin="4dp"
                android:text="@string/btnEco" />

            <Button
                android:id="@+id/buttonVeloVole"
                android:layout_width="70dp"
                android:layout_height="70dp"
                android:layout_columnWeight="1"
                android:layout_margin="4dp"
                android:text="@string/bike_stolen" />

            <Button
                android:id="@+id/buttonVeloTrouve"
                android:layout_width="70dp"
                android:layout_height="70dp"
                android:layout_columnWeight="1"
                android:layout_margin="4dp"
                android:text="@string/bike_found" />

        </GridLayout>

        <ImageView
            android:id="@+id/bikefront"
            android:layout_width="160dp"
            android:layout_height="205dp"
            android:contentDescription="@string/back_button"
            android:src="@drawable/bikefront"
            app:layout_constraintBottom_toTopOf="@+id/lvModules"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.497"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/buttonContainer"
            app:layout_constraintVertical_bias="0.528" />

        <TextView
            android:id="@+id/textviewBle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingLeft="15dp"
            android:text="@string/ble_list"
            android:textStyle="bold"
            android:fontFamily="@font/rubik_bold"
            app:layout_constraintBottom_toTopOf="@+id/lvModules"
            app:layout_constraintStart_toStartOf="@+id/lvModules" />

        <ListView
            android:id="@+id/lvModules"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:layout_below="@id/buttonContainer"
            android:divider="@android:color/darker_gray"
            android:dividerHeight="1dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/buttonContainer"
            app:layout_constraintVertical_bias="0.777"
            tools:layout_editor_absoluteX="16dp" />

        <Button
            android:id="@+id/buttonShowPin"
            android:layout_width="116dp"
            android:layout_height="45dp"
            android:layout_marginEnd="56dp"
            android:text="Show PIN"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="1.0"
            app:layout_constraintStart_toEndOf="@+id/buttonScanner"
            app:layout_constraintTop_toBottomOf="@+id/lvModules"
            app:layout_constraintVertical_bias="0.722" />

        <Button
            android:id="@+id/buttonScanner"
            android:layout_width="116dp"
            android:layout_height="45dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginStart="44dp"
            android:text="@string/scan"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/lvModules"
            app:layout_constraintVertical_bias="0.707" />


        <!-- Autres éléments comme ListView et EditText -->

    </androidx.constraintlayout.widget.ConstraintLayout>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigation_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        app:menu="@menu/nav_menu"
        app:headerLayout="@layout/nav_header" />

</androidx.drawerlayout.widget.DrawerLayout>