# 🔐 Clé secrète pour générer les tokens JWT (CHANGER cette valeur en production)
SECRET_KEY=$(openssl rand -hex 32)

# 🎯 Paramètres de connexion à la base de données MySQL (À PERSONNALISER)
DB_HOST=localhost       # Adresse du serveur MySQL (ex: localhost, 127.0.0.1, ou IP du serveur distant)
DB_USER=tpreseau        # Nom d'utilisateur MySQL (ex: root, admin, ou ton propre utilisateur)
DB_PASSWORD=tpreseau    # Mot de passe MySQL (À REMPLACER par ton propre mot de passe)
DB_NAME=biketrack       # Nom de la base de données utilisée

# 🌍 Port utilisé par le serveur HTTPS (Changer si nécessaire)
HTTPS_PORT=3003         # Port sur lequel l'API va écouter (ex: 443 en production)

# 🔑 Chemins des certificats SSL (À MODIFIER avec les fichiers réels)
SSL_KEY_PATH=/etc/ssl/private/selfsigned.key   # Clé privée pour HTTPS (fournie par Let's Encrypt ou autre)
SSL_CERT_PATH=/etc/ssl/certs/selfsigned.crt    # Certificat SSL

# ⚠️ Mode de l'application (utile pour le débogage)
APP_ENV=development    # Modes possibles : development, production
